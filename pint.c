#include "monty.h"

/**
 * pint - prints the top of the stack
 *
 * @top: The top item in the stack
 *
 * Return: top
 */

stack_t *pint(stack_t **head)
{
	fprintf(stdout, "%d\n", (*head)->n);

	return (top);
}
